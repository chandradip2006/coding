#include<stdio.h>
int main(){
    float radius = 4.79;
    float pi = 3.141592;
    float volume = (4.0 / 3.0) * pi * radius * radius * radius ;
    printf("the volume of the sphere is %f" , volume);
    return 0 ;
}